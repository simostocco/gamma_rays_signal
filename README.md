# gamma_rays_signal
Physics-driven binary classification on the MAGIC Cherenkov Telescope dataset. Model selection uses partial AUC at very low false-positive rates (FPR≤0.01–0.10), with leakage-safe pipelines, nested cross-validation, and threshold selection at fixed FPR.
